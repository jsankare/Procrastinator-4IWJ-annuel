{
  "name": "procrastinator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up -d",
    "dev-b": "docker compose -f docker-compose.dev.yml up -d --build",
    "down": "docker compose -f docker-compose.dev.yml down",
    "down-v": "docker compose -f docker-compose.dev.yml down -v",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "format": "pnpm -r format",
    "docker:nuke": "docker stop $(docker ps -aq) 2>/dev/null || true && docker rm -f $(docker ps -aq) 2>/dev/null || true && docker rmi -f $(docker images -aq) 2>/dev/null || true && docker volume rm -f $(docker volume ls -q) 2>/dev/null || true && docker network rm $(docker network ls -q) 2>/dev/null || true && docker system prune -a --volumes -f",
    "docker:reset": "pnpm docker:nuke && pnpm dev-b",
    "test:front": "cd frontend && pnpm run test:e2e",
    "test:auth": "cd services/auth-service && pnpm test",
    "test:2fa": "cd services/auth-service && NODE_OPTIONS='--experimental-vm-modules' pnpm exec jest --testPathPatterns=2fa",
    "test:workspace": "cd services/workspace-service && pnpm test",
    "test:task": "cd services/tasks-service && npx test",
    "test:services": "pnpm test:workspace && pnpm test:2fa",
    "test:all": "pnpm test:services && pnpm test:front"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}